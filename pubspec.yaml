name: infection
description: "An implementation of the Chain Reaction game in Flutter."
author: "David Mougey <imapiekindaguy@gmail.com>"
version: 0.1.0
homepage: "https://github.com/ddmgy/infection"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.1.0
  equatable: ^1.0.0
  flutter_colorpicker: ^0.3.0
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
